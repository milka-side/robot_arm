# Parameters for keyboard_servo_node when teleoperating the Gazebo sim.
# use_sim_time follows Gazebo's /clock; speeds are raised above the
# conservative hardware defaults, sim-only.
keyboard_servo_node:
  ros__parameters:
    use_sim_time: true
    linear_speed: 0.25
    angular_speed: 0.75
